#ifndef CSTRING_HH
#define CSTRING_HH

#include <_size_t.hh>

namespace std
{

extern "C" void *memcpy(void *, const void *, std::size_t);
extern "C" void *memset(void *, int, std::size_t);

}

#endif
